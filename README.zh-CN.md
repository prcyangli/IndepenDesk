# IndepenDesk

[English](README.md) | [Türkçe](README.tr.md) | [Deutsch](README.de.md) | [Français](README.fr.md) | [Italiano](README.it.md) | [Русский](README.ru.md) | **中文** | [日本語](README.ja.md)

**Windows 上每个显示器独立的虚拟桌面** — 相当于 macOS 的"显示器具有单独的空间"功能。

本地功能修改、验证结果和待修复问题请参阅 [项目修改与审查记录](PROJECT_NOTES.zh-CN.md)。

## 问题

Windows 的虚拟桌面是全局的：按 `Win+Ctrl+←/→` 会**同时切换所有显示器**。而在 macOS 上，每个屏幕都有自己的空间（Spaces），只有光标所在的屏幕会切换。IndepenDesk 把这种体验带到了 Windows。

## 功能

- 🖥 **每个显示器独立的桌面** — 切换只影响鼠标所在的显示器，其他显示器保持不变。
- ➕ **动态且独立的桌面数量** — 每个显示器从 1 个桌面开始；在末尾向右移动会新建桌面（每个显示器最多 9 个）。自动创建但未使用的末尾桌面会在离开后清理；在总览中手动添加的空桌面会保留到手动关闭。
- 🔢 **全局数字快捷键** — `Ctrl+Alt+数字` 按屏幕顺序跨显示器跳转；总览和 OSD 使用更直观的显示器内编号。
- 🔔 **简洁的屏幕提示** — 每次切换后显示，不播放可能抢焦点的滑动动画。
- 🗂 **总览** (`Ctrl+Alt+↑`) — 类似 Mission Control 的网格视图，支持拖放：在桌面和显示器之间移动窗口，将整个桌面移到另一台显示器，右键移动菜单。
- 📌 **共用任务栏**（可选，托盘菜单）— 所有桌面的窗口都保留在任务栏和 Alt-Tab 中；点击任一窗口即可直接跳转到它所在的桌面。
- 🧲 **任务栏跳转**（默认开启，托盘菜单）— 点击正在运行的单实例应用（如即时通讯软件）的任务栏/托盘图标时，切换到该窗口所在的桌面，而不是把窗口拉到当前桌面。
- 🌍 **8 种语言** — 自动检测，可在托盘菜单中更改。
- 🔄 通过 GitHub Releases **检查更新**。
- 🚀 **默认开机自动启动** — 可随时在托盘菜单中关闭。
- 🛟 **防崩溃** — 隐藏的窗口会记录到磁盘，下次启动时自动恢复。

## 安装

支持 Windows 10 (1607+) 和 Windows 11 的 **x64、x86 和 ARM64**。所有安装包均为独立部署 — 无需安装 .NET 运行时。

本 fork 的包管理器条目尚未发布。在发布前请只使用下面 `prcyangli` 仓库的 Release 文件；原上游 winget 包安装的是另一份代码。

**安装程序（推荐）：** 从 [Releases](https://github.com/prcyangli/IndepenDesk/releases) 下载 `IndepenDesk-Setup-<版本>-<架构>.exe` 并运行 — 可选桌面图标，提供 7 种安装语言。

**MSI**（企业 / GPO 部署）：`IndepenDesk-<版本>-<架构>.msi`。

**便携版：** `IndepenDesk-v<版本>-win-<架构>.zip` — 解压即用，无需安装。


程序驻留在系统托盘（两个蓝色屏幕图标）。

## 快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+Alt+←` / `Ctrl+Alt+→` | 切换鼠标所在显示器的上一个 / 下一个桌面（在末尾按 `→` 新建） |
| `Ctrl+Alt+↑` | 打开/关闭总览 |
| `Ctrl+Alt+1..9` | 跳转到对应全局编号的桌面 |
| `Ctrl+Alt+Shift+←/→` | 将当前窗口移到相邻桌面并跟随 |

每次切换时屏幕会显示指示器（例如“桌面 2”）。托盘菜单中的**使用说明**包含动画手势指南。

## 触摸板（macOS 风格滑动）

默认情况下四指滑动会触发 Windows 的**全局**桌面切换。覆盖方法：

1. 打开 **设置 → 蓝牙和其他设备 → 触摸板 → 高级手势**。
2. 为四指滑动的每个方向选择**自定义快捷方式**并录制：
   - 向左滑动 → `Ctrl+Alt+←`，向右滑动 → `Ctrl+Alt+→`，向上滑动 → `Ctrl+Alt+↑`
3. 录制自定义快捷方式后会自动替换 Windows 默认行为 — 只有光标所在的显示器会切换。

## 工作原理

IndepenDesk 不使用（也无法修改）Windows 的全局虚拟桌面系统。它为每个显示器维护窗口集合，切换时只隐藏/显示该显示器的窗口（`ShowWindow`）。隐藏的窗口也会从任务栏和 Alt-Tab 中消失，因此感觉就像真正的桌面切换。新窗口会自动归入其所在显示器的当前桌面。

开启**任务栏跳转**（默认开启，托盘菜单）后，单实例应用重新显示其隐藏窗口时（点击任务栏固定图标、托盘图标或通知），IndepenDesk 会跳转到该窗口所在的桌面，而不是把窗口收进当前桌面，与 Windows 原生虚拟桌面的行为一致。若应用新建窗口、或只显示而不激活窗口，则仍归入当前桌面。

可选的**共用任务栏**模式（托盘菜单）不再隐藏非当前桌面的窗口，而是把它们原地最小化停靠。所有桌面的窗口因此保留在任务栏和 Alt-Tab 中，激活其中任何一个都会跳转到它所在的桌面。停靠位置与隐藏状态一样记录到磁盘日志，崩溃后重启会自动还原。

## 已知限制

- 以管理员权限运行的程序窗口无法隐藏，除非 IndepenDesk 本身以管理员身份运行。
- 触摸板的"自定义快捷方式"手势属于合成按键：当焦点位于管理员（提权）窗口上时，会被 Windows 安全机制（UIPI）丢弃，手势因此失效。实体键盘快捷键和托盘菜单不受影响，此时请改用它们。
- 共用任务栏模式下，停靠窗口被原地最小化（停止渲染）。若该模式下进程被强制结束，窗口只是保持最小化——从任务栏手动恢复即可；再次启动 IndepenDesk 0.4.4 或更高版本会自动重新接管（旧版本无法读取 v3 日志格式）。
- 系统原生的 `Win+Ctrl+←/→` 仍会触发全局切换 — 不使用即可。
- `Ctrl+Alt+←/→` 可能与 Intel 显卡的"旋转屏幕"热键冲突；如有需要请在 Intel 设置中禁用。
- Windows 11 任务栏的右键菜单无法被第三方扩展；请使用总览中的右键菜单。

## 许可证

[MIT](LICENSE)
