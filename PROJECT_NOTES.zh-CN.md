# IndepenDesk 本地修改与审查记录

更新日期：2026-09-11

本文记录基于 `main` 分支（原始基线提交 `21c9b74`）完成的本地功能修改、验证结果，以及代码审查中发现但尚未修复的问题。

## 本次已完成修改

### 总览窗口布局与显示

- 将桌面卡片由 `276 × 216` 调整为 `384 × 280`，同时放大“＋”卡片、桌面标题、当前桌面标记和窗口条目。
- 每个桌面卡片中的窗口列表改为独立纵向滚动；窗口较多时可在卡片内部上下滚动，不再仅显示固定数量后用“更多”代替。
- 窗口条目高度根据当前系统字体与 DPI 动态计算，改善高分辨率和中文字体环境下的文字裁切。
- 窗口标题不再预先截断固定字符数，改为根据实际可用宽度显示省略号，并通过提示框展示完整标题。
- 窗口图标改用等比例缩放并扩大显示区域；图标、文字和拖拽手柄使用独立布局，避免后方有空间但图标仍被裁切。
- 总览主要文字改用系统消息框字体，提升中文字体回退和高 DPI 环境的兼容性。
- 重建总览界面时主动释放旧控件，减少图标位图和控件资源残留。

### 总览窗口关闭行为

- 新增右上角关闭按钮，并放大为 `72 × 60`。
- 关闭图标改为矢量绘制的两条交叉线，避免字体字形在高 DPI 下被裁切。
- 保留 `Esc` 关闭和总览窗口失去焦点时自动关闭的原有行为。
- 桌面切换、窗口移动、桌面移动和新建桌面等总览内部操作完成后，总览窗口保持打开并刷新内容。
- 内部操作期间临时抑制失焦关闭，操作结束后重新激活总览窗口。

### 新建桌面与拖拽行为

- 点击“＋”只创建新桌面，不再立即切换到新桌面。
- 将窗口拖到“＋”时，新建桌面并把该窗口移入新桌面，但不切换过去。
- 将桌面卡片拖到“＋”或显示器行时，可将整个桌面移动到目标显示器的末尾。
- 同一显示器内可拖动桌面卡片标题调整桌面顺序。
- 将桌面拖到其他桌面卡片的左侧或右侧，可精确指定跨显示器移动后的插入位置。
- 重排或跨显示器移动时保持原当前桌面的身份，避免因为列表索引变化而错误切换当前桌面。

### 窗口管理与本地验证

- 窗口过滤黑名单增加 `Xaml_WindowedPopupClass` 和 `COMTASKSWINDOWCLASS`，减少 XAML 临时弹窗和“主机弹出窗口”被当作普通应用窗口管理。
- 默认关闭桌面切换滑动动画；桌面切换仍保留 OSD 指示。
- Debug 构建不修改开机启动注册表，并在启动后自动打开总览，便于本地反复验证。
- 更新 8 种界面语言中的“新建桌面”提示；中文总览说明同步补充重排、拖到“＋”、保持总览和关闭按钮行为。

## 尚未修复的问题

以下项目来自本次静态审查和多架构编译检查，只做了记录，尚未修改代码。

### 高优先级

- **崩溃恢复仅以 HWND 识别窗口。** `hidden.json` 只保存窗口句柄。Windows 可能复用句柄，下一次启动存在显示其他进程隐藏窗口的风险。建议同时记录并校验 PID、进程启动时间和窗口类名。
- **恢复日志不是原子写入。** 进程在 `File.WriteAllText` 期间退出可能得到空文件或损坏文件，削弱“崩溃安全”能力。建议临时文件写入后原子替换，并记录失败日志。
- **x86 图标回退调用错误。** 当前回退路径始终调用 `GetClassLongPtrW`，而 32 位 `user32.dll` 只提供 `GetClassLongW`；异常被捕获后表现为部分窗口无图标。需要增加 32/64 位分支。
- **没有统一的异常退出清理。** UI 线程未处理异常可能绕过 `ExitThreadCore`，导致非当前桌面的窗口继续隐藏，直到程序重新启动并成功恢复。

### 中优先级

- **已收录窗口不会重新执行完整资格检查。** 同步清理只移除失效 HWND；窗口后来变为工具窗、系统弹窗、无标题窗口或 cloaked 状态后仍可能留在桌面集合中。当前窗口类黑名单仍属于容易遗漏的启发式方案。
- **手动新建的空桌面无法删除。** 为了让“新建但不切换”后的空桌面不被同步清理，代码将其永久保留到本次进程结束；界面和管理器目前都没有删除桌面的功能。
- **跨不同缩放比例显示器移动窗口时没有 DPI 换算。** 当前保留物理像素宽高，从 100% 移到 150%/200% 显示器时，窗口视觉尺寸可能改变。
- **总览和帮助界面存在资源释放缺口。** `ToolTip`、临时 `ContextMenuStrip`、OSD 定时器以及帮助动画中的 `StringFormat` 没有全部显式释放，长期运行时可能增加 GDI/原生资源占用。
- **传统安装程序和 MSI 的权限范围不一致。** WiX 包声明为 `perMachine`，Inno Setup 默认也以管理员模式安装，但两者的开机启动均写入 `HKCU`；使用管理员账户或 GPO 部署时可能只影响安装者账户。Inno Setup 6.7.3 实际编译时也给出了 `UsedUserAreasWarning`。
- **总览布局仍以固定像素卡片为主。** 桌面数量多或 DPI 较高时可能出现较长的横向滚动、标题和右上角关闭按钮争用空间等响应式布局问题。

### 较低优先级与维护不足

- 窗口集合使用 `HashSet`，总览中的窗口显示顺序没有稳定保证。
- 每台显示器最多 9 个桌面，但全局数字快捷键只注册 `1..9`；总编号 10 及以上没有数字直达快捷键。
- 开机启动菜单状态来自 `settings.json`，没有读取实际注册表或 Windows 启动审批状态，外部修改后菜单勾选可能不准确。
- 当前代码已经关闭滑动动画，但各语言 README 仍将其列为启用功能。
- README 仍写着“末尾空桌面自动删除”，与手动创建的保留空桌面行为不完全一致。
- 单实例提示仍为硬编码土耳其语，没有进入本地化资源。
- 设置、启动、恢复等多处异常被静默吞掉，项目没有日志文件或诊断入口。

## 测试与构建记录

- `git diff --check`：通过，仅有 Git 的 LF/CRLF 转换提示。
- Release `win-x64`：编译成功，0 个普通警告，0 个错误。
- Release `win-x86`：编译成功，0 个普通警告，0 个错误。
- Release `win-arm64`：编译成功，0 个普通警告，0 个错误。
- 启用全部 .NET 分析规则：0 个错误、68 个警告。多数为 P/Invoke 加固和代码风格建议，其中资源释放警告具有实际修复价值。
- 仓库当前没有自动化测试；CI 仅执行普通 Release 编译，未覆盖桌面状态变化、跨屏拖拽、异常恢复和安装包烟雾测试。
- 本地自包含发布文件输出到 `publish/x64/`，本地 Inno Setup 安装包输出到 `artifacts/`；两个目录均不纳入 Git 提交。
- 本机 x64 自包含单文件发布和 Inno Setup EXE 安装包均编译成功。当前本地安装包未进行代码签名，Windows SmartScreen 可能在首次运行时提示未知发布者。

## 建议处理顺序

1. 加固崩溃恢复记录和统一异常退出清理。
2. 修复 x86 `GetClassLongPtr` 回退调用。
3. 重构窗口资格检查，减少依赖窗口类黑名单。
4. 增加桌面删除及保留空桌面的生命周期规则。
5. 修复 DPI 换算和 UI/GDI 资源释放。
6. 增加桌面管理单元测试、窗口过滤测试及安装包烟雾测试。
